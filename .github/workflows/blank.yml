name: CI
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo '${{ secrets.RCLONE_CONF }}' | base64 -d > rclone.conf
      - uses: valeriangalliat/action-sshd-cloudflared@v1
